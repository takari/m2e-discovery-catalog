# Generating the M2E discovery catalog

To generate the catalog checkout all the Git repositories that contain catalog entries into this directory and then run the following script:

```
./catalog-maker.rb
```

